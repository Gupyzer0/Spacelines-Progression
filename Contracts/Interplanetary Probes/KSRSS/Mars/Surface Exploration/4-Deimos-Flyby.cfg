CONTRACT_TYPE:NEEDS[KSRSS]
{
	name = flybyDeimos
	title = Deimos Flyby (Uncrewed)
	group = MarsSurfaceExploration

	description = <b>Program: Mars Surface Exploration</b><br><br>Create a probe and send it to flyby Deimos within 45 km. Make sure to transmit science from the flyby.
	synopsis = Flyby Deimos closer than 45 km
	completedMessage = Congratulations on the flyby!

	cancellable = true
	declinable = true
	autoAccept = false
	minExpiry = 0
	maxExpiry = 0
	maxCompletions = 1
	maxSimultaneous = 1
	deadline = 1460

	targetBody = Deimos

	// ------------------------------------------------------------------------
	// Rewards
	// ------------------------------------------------------------------------
	prestige = Significant
	advanceFunds = @rewardReputation * @SpacelinesContracts:MarsVenusSurfaceExplorationMultiplier * 0.4
	rewardFunds = @rewardReputation * @SpacelinesContracts:MarsVenusSurfaceExplorationMultiplier * 0.6
	failureFunds = @advanceFunds
	rewardScience = 5
	rewardReputation = 200
	failureReputation = @rewardReputation * 0.5

	// ------------------------------------------------------------------------
	// Requirements
	// ------------------------------------------------------------------------

	REQUIREMENT
    {
        name = CompleteContract
        type = CompleteContract

        contractType = orbitMars
    }

	// ------------------------------------------------------------------------
	// Parameters
	// ------------------------------------------------------------------------

	PARAMETER
	{
		name = VesselGroup
		type = VesselParameterGroup
		title = Flyby Deimos
		define = FlybyDeimos

		PARAMETER
		{
			name = Crewmembers
			type = HasCrew
			minCrew = 0
			maxCrew = 0
			title = Uncrewed
			hideChildren = true
		}
		PARAMETER
		{
			name = FlybyPlanet
			type = ReachState
			maxAltitude = 45000
			disableOnStateChange = true
			title = Flyby Deimos within 45 km
			hideChildren = true
		}
		PARAMETER
		{
			name = CollectSpaceScience
			type = CollectScience
			title = Transmit science data from space near @targetBody

			recoveryMethod = Transmit
			hideChildren = true
			completeInSequence = true
		}
	}
}