// Mercury Launch Escape System test
CONTRACT_TYPE
{
    name = Little-Joe-1
	group = MercuryMission
	title = Little Joe 1

	description = Little Joe 1 is a solid-fuel rocket that was designed for a Max Q abort and launch escape system test for the Mercury capsule. The objective is to determine how well the escape rocket would function under the most severe dynamic loading conditions anticipated during a Mercury-Atlas launching.
	
	notes = Little-Joe Mercury was first launched on August 21, 1959 and the first succesfull test launched on December 4, 1959, from the Wallops Flight Facility.
	synopsis = <color=yellow>Launch the Mercury capsule atop a Little Joe booster on a sub-orbital trajectory and test the launch escape system.</color>
	completedMessage = Mission Success!

	agent = nasa
	deadline = 0
	prestige = trivial
	minExpiry = 0
	maxExpiry = 0
	cancellable = false
	declinable = false
	maxCompletions = 1
	maxSimultaneous = 1
	rewardScience = 2
	rewardReputation = 5
	rewardFunds = 2500
	failureFunds = 10000
	advanceFunds = 2500

	PARAMETER
	{
		name = Mercury-Little-Joe-1
		type = VesselParameterGroup
		title = Launch the Mercury capsule atop a Little Joe booster on a sub-orbital trajectory and test the launch escape system.
		define = Mercury-Little-Joe-1

		PARAMETER 
		{
			name = NewVessel
			type = NewVessel
			hidden = true
		}

		PARAMETER
		{
			name = Crewmembers
			type = HasCrew
			minCrew = 0
			maxCrew = 0
		}

        // Required Parts ---------------------------------
		PARAMETER
		{
			type = PartValidation
			part = bluedog_Mercury_Capsule
			disableOnStateChange = True
		}
        PARAMETER
		{
			type = PartValidation
			part = bluedog_Mercury_Heatshield
			disableOnStateChange = True
		}
        PARAMETER
		{
			type = PartValidation
			part = bluedog_Mercury_ParachuteRCS
			disableOnStateChange = True
		}
        PARAMETER
		{
			type = PartValidation
			part = bluedog_Mercury_Parachute
			disableOnStateChange = True
		}
        PARAMETER
		{
			type = PartValidation
			part = bluedog_Mercury_AntennaNose
			disableOnStateChange = True
		}
        PARAMETER
		{
			type = PartValidation
			part = bluedog_Mercury_LES
			disableOnStateChange = True
		}
		// /Required Parts ---------------------------------
	}

	PARAMETER
	{
		name = ReachState
		type = ReachState

		minSpeed = 350
		title = Reach Mach 1 and activate the LES
		disableOnStateChange = True
	}
	
	PARAMETER
	{
        name = ReturnHome
        type = ReturnHome
		title = Recover the Mercury capsule intact
	}

	REQUIREMENT
	{
		name = CompleteContract
		type = CompleteContract
		contractType = Pioneer-4
		minCount = 1
		cooldownDuration = 0d
	}
}