CONTRACT_TYPE
{
	name = Pioneer-1
	group= PioneerMission
	title = Pioneer 1
	
	description = Pioneer 1 is going to be the first probe by newly formed NASA, is intended to study the ionizing radiation, cosmic rays, magnetic fields, and micrometeorites in the vicinity of the Earth and in lunar orbit.
	
	notes = Due to a launch vehicle malfunction, the spacecraft attained only a ballistic trajectory and never reached the Moon. It did return data on the near-Earth space environment. Pioneer 1 was launched on a Thor-Able I on October 11, 1958 from the Cape Canaveral Launch Complex.
	synopsis =  <color=yellow>Launch Pioneer-1 on a ballistic trajectory towards the Mun. Orbit the Mun if you're able to do so.</color>
	completedMessage = Mission Success!

	agent = nasa
	prestige = trivial
	minExpiry = 90
	maxExpiry = 90
	cancellable = false
	declinable = false
	maxCompletions = 1
	maxSimultaneous = 1
	rewardScience = 5
	rewardReputation = 5
	rewardFunds = 10000
	failureFunds = 30000
	advanceFunds = 8000

	PARAMETER
	{
        name = Pioneer-1
        type = VesselParameterGroup
        title = Launch the Pioneer 1 probe to the Mun.
        define = Pioneer-1

		PARAMETER 
		{
			name = NewVessel
			type = NewVessel
			hidden = true
		}

		PARAMETER
		{
			name = Crewmembers
			type = HasCrew
			minCrew = 0
			maxCrew = 0
		}

		
		// Required Parts ---------------------------------
		PARAMETER
		{
			type = PartValidation
			part = bluedog_Pioneer_1
			disableOnStateChange = True
		}
		// /Required Parts ---------------------------------

		PARAMETER
		{
			name = Orbit
			type = Orbit

			situation = SUB_ORBITAL
			targetBody = HomeWorld()
			minApA = 27500000
			disableOnStateChange = true
		}

		PARAMETER
		{
			name = Orbit
			type = Orbit

			optional = true
			targetBody = HomeWorld().Children().First()
			situation = ORBITING
			title = Establish a Lunar orbit.
			minApA = 1000
			minPeA = 1000
			rewardScience = 100.0
			rewardReputation = 20.0
			disableOnStateChange = true
		}

		PARAMETER
		{
			type = CollectScience

			targetBody = HomeWorld()
			situation = InSpaceLow
			location = Space
			experiment = bd_GeigerCounter
			recoveryMethod = Transmit
			disableOnStateChange = true
		}

		PARAMETER
		{
			type = CollectScience

			targetBody = HomeWorld()
			situation = InSpaceHigh
			location = Space
			experiment = bd_GeigerCounter
			recoveryMethod = Transmit
			disableOnStateChange = true
		}

		PARAMETER
		{
			type = CollectScience
			
			optional = true
			targetBody = HomeWorld().Children().First()
			location = Space
			experiment = bd_GeigerCounter
			recoveryMethod = Transmit
			title = Take and transmit the Geiger Counter measurements from the Mun.
			disableOnStateChange = true
		}
	}

	REQUIREMENT
	{
        name = CompleteContract
        type = CompleteContract
        contractType = Explorer-1
        minCount = 1
        maxCount = 1
        cooldownDuration = 0d
	}
}